<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.marskyer.tuski.moonfinder" >

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".appmenu" >
        </activity>
        <activity android:name=".features_menu" >
        </activity>
        <activity
            android:name=".circular_view"
            android:label="@string/title_activity_circular_view" >
        </activity>
        <activity android:name=".compass" >
        </activity>

        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
        <uses-permission android:name="android.permission.INTERNET" />

        <activity
            android:name=".show_pictures"
            android:label="@string/title_activity_show_pictures" >
        </activity>
        <activity
            android:name=".phase_view"
            android:label="@string/title_activity_phase_view" >
        </activity>
    </application>

</manifest>
